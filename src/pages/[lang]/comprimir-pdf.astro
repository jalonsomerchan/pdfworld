---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CompressPdfTool from '../../components/CompressPdfTool.svelte';
import { getLocalizedPath, isLang, useTranslations, type Lang } from '../../i18n/utils';
import { locales } from '../../i18n/ui';

export function getStaticPaths() {
  return locales.map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params;

if (!isLang(lang)) {
  return Astro.redirect(getLocalizedPath('es', 'comprimir-pdf'));
}

const t = useTranslations(lang as Lang);
---

<BaseLayout
  lang={lang}
  title={t('tool.compress.seo.title')}
  description={t('tool.compress.seo.description')}
  path="comprimir-pdf"
>
  <section class="hero">
    <span class="badge">{t('home.privacyBadge')}</span>
    <h1>{t('tool.compress.h1')}</h1>
    <p class="lead">{t('tool.compress.lead')}</p>
  </section>

  <CompressPdfTool client:load lang={lang} />

  <section aria-labelledby="how-title">
    <h2 id="how-title">{t('tool.compress.howTitle')}</h2>
    <ol>
      <li>{t('tool.compress.step1')}</li>
      <li>{t('tool.compress.step2')}</li>
      <li>{t('tool.compress.step3')}</li>
    </ol>
  </section>

  <section>
    <h2>{t('tool.compress.privacyTitle')}</h2>
    <p>{t('tool.compress.privacyText')}</p>
  </section>

  <Fragment slot="footer">{t('footer.privacy')}</Fragment>
</BaseLayout>
